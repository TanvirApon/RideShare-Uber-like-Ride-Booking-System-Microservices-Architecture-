spring:
  application:
    name: location-service
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoint:
    web:
      exposure:
        include: health,info

server:
  port: 8082